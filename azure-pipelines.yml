trigger:
- main

pool:
  name: Default  # Or your custom agent pool name if you changed it

steps:
- script: echo "✅ Pipeline executed successfully using self-hosted agent"
  displayName: 'Test Agent Job'
