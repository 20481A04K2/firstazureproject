trigger:
- main

pool:
  vmImage: ubuntu-latest  # Uses the Hosted Agent shown in your screenshot

steps:
- script: echo "✅ Azure Pipeline is working with Hosted Agent!"
  displayName: "Print test message"
